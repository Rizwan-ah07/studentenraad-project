<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Reset Password - Student Council Portal</title>

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/reset.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>

    <main class="container reset-password-container">
        <section class="row justify-content-center">
            <article class="col-md-5">
                <header class="text-center">
                    <img src="/images/logo.png" alt="Logo" class="brand-logo">
                </header>
                <article class="form-card">
                    <h3 class="text-center">Reset Your Password</h3>
                    <% if (message) { %>
                        <aside class="alert alert-success mt-3">
                            <%= message %>
                        </aside>
                    <% } %>
                    <% if (error) { %>
                        <aside class="alert alert-danger mt-3">
                            <%= error %>
                        </aside>
                    <% } %>
                    <% if (token) { %>
                        <form action="/reset-password" method="POST" class="mt-4" id="resetPasswordForm">
                            <input type="hidden" name="token" value="<%= token %>">
                            <fieldset class="form-group password-section">
                                <label for="newPassword" class="mb-2">New Password</label>
                                <input type="password" class="form-control" id="newPassword" name="newPassword" required>
                                <!-- Password Requirements Dropdown -->
                                <div id="passwordHelp" class="password-requirements">
                                    <ul>
                                        <li id="length" class="invalid">At least 8 characters</li>
                                        <li id="uppercase" class="invalid">At least one uppercase letter</li>
                                        <li id="number" class="invalid">At least one number</li>
                                        <li id="symbol" class="invalid">At least one special symbol (!@#$%^&*(),.?":{}|<>)</li>
                                    </ul>
                                </div>
                            </fieldset>
                            <fieldset class="form-group">
                                <label for="confirmPassword" class="mb-2">Confirm New Password</label>
                                <input type="password" class="form-control" id="confirmPassword" name="confirmPassword" required>
                                <div id="passwordError" class="error-message">
                                    Passwords do not match.
                                </div>
                            </fieldset>
                            <button type="submit" class="btn btn-primary btn-block">Reset Password</button>
                        </form>
                    <% } %>
                </article>
            </article>
        </section>
    </main>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/[email protected]/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>
    <!-- Include your custom JS for password validation -->
    <script src="/js/passwordValidation.js"></script> <!-- Ensure this path is correct -->
</body>
</html>