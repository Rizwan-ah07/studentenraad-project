<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login - Student Council Portal</title>

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/reset.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>

    <main class="container login-container">
        <section class="row justify-content-center">
            <article class="col-md-5">
                <header class="text-center">
                    <img src="/assets/images/AP_logo_staand_rgb.png" alt="Logo" class="brand-logo">
                </header>
                <article class="form-card">
                    <h3 class="text-center">Welcome Back</h3>
                    <% if (message) { %>
                        <aside class="alert alert-success mt-3">
                            <%= message %>
                        </aside>
                    <% } %>
                    <% if (error) { %>
                        <aside class="alert alert-danger mt-3">
                            <%= error %>
                        </aside>
                    <% } %>
                    <form action="/login" method="POST" class="mt-4">
                        <fieldset class="form-group">
                            <label for="loginIdentifier" class="mb-2">Email or Username</label>
                            <input type="text" class="form-control" id="loginIdentifier" name="loginIdentifier" required autofocus>
                        </fieldset>
                        <fieldset class="form-group">
                            <label for="password" class="mb-2">Password</label>
                            <input type="password" class="form-control" id="password" name="password" required>
                        </fieldset>
                        <button type="submit" class="btn btn-primary btn-block">Login</button>
                        <p class="text-center mt-3">
                            Don't have an account? <a href="/register">Register here</a>
                        </p>
                        <p class="text-center mt-2">
                            <a href="/forgot-password">Forgot your password?</a>
                        </p>
                    </form>
                </article>
            </article>
        </section>
    </main>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/[email protected]/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>

</body>
</html>